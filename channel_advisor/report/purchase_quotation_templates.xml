<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="report_purchasequotation_document_inherit">
        <t t-call="web.external_layout">
            <t t-set="o" t-value="o.with_context(lang=o.partner_id.lang)" />
            <t t-set="address">
                <div t-field="o.partner_id"
                    t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}' />
                <p t-if="o.partner_id.vat">
                    <t t-esc="o.company_id.country_id.vat_label or 'Tax ID'" />: <span t-field="o.partner_id.vat" /></p>
            </t>
            <t t-if="o.dest_address_id">
                <t t-set="information_block">
                    <strong>Shipping address:</strong>
                    <div t-field="o.dest_address_id"
                        t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'
                        name="purchase_shipping_address" />
                </t>
            </t>
            <div class="page">
                <div class="oe_structure" />

                <h2>Request for Quotation <span t-field="o.name" /></h2>

                <table class="table table-sm">
                    <thead>
                        <tr>
                            <th class="text-left" style="font-size: 13px;">SKU</th>
                            <th class="text-left" style="font-size: 13px;">Vendor SKU</th>
                            <th style="font-size: 13px;"><strong>Product</strong></th>
                            <th name="th_expected_date" class="text-center" style="font-size: 13px;"><strong>Expected
                                    Date</strong></th>
                            <th name="th_quantity" class="text-right" style="font-size: 13px;"><strong>Qty</strong></th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="o.order_line" t-as="order_line">
                            <tr
                                t-att-class="'bg-200 font-weight-bold o_line_section' if order_line.display_type == 'line_section' else 'font-italic o_line_note' if order_line.display_type == 'line_note' else ''">
                                <t t-if="not order_line.display_type">
                                    <td style="font-size: 12px;">
                                        <span t-field="order_line.product_id.default_code" />
                                    </td>
                                    <td style="font-size: 12px;">
                                        <span t-field="order_line.vendor_sku" />
                                    </td>
                                    <td id="product" style="font-size: 12px;">
                                        <span t-field="order_line.name" />
                                    </td>
                                    <td class="text-center" style="font-size: 12px;">
                                        <span t-field="order_line.date_planned" />
                                    </td>
                                    <td class="text-right" style="font-size: 12px;">
                                        <span t-field="order_line.product_qty" />
                                        <span t-field="order_line.product_uom" groups="uom.group_uom" />
                                    </td>
                                </t>
                                <t t-else="">
                                    <td colspan="99" id="section">
                                        <span t-field="order_line.name" />
                                    </td>
                                </t>
                            </tr>
                        </t>
                    </tbody>
                </table>

                <p t-field="o.notes" />

                <div class="oe_structure" />
            </div>
        </t>
    </template>

    <template id="purchase.report_purchasequotation">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="o">
                <t t-call="channel_advisor.report_purchasequotation_document_inherit" t-lang="o.partner_id.lang" />
            </t>
        </t>
    </template>
</odoo>
