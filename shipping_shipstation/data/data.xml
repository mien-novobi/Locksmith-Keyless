<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">

        <record id="email_template_shipstaion_sync_fail" model="mail.template">
            <field name="name">Error Log</field>
            <field name="reply_to"></field>
            <field name="email_to"></field>
            <field name="subject">Shipstation Sync failed</field>
            <field name="model_id" ref="shipping_shipstation.model_shipstation_log" />
            <field name="auto_delete" eval="True" />
            <field name="body_html">
                <![CDATA[   Hello  <br/>
            Shipstation Sync Failed!  Please find the details as below.<br/>
            <p>
            % if object.date:
                Date : ${object.date}<br/>
            % endif
            % if object.res_model:
                Model : ${object.res_model}<br/>
            % endif
            % if object.operation:
                Operation : ${object.operation}<br/>
            % endif
            % if object.res_id:
                ERP ID : ${object.res_id}<br/>
            % endif
            % if object.shipstation_id:
                Shipstation ID : ${object.shipstation_id}<br/>
            % endif
            % if object.company_id:
                Company : ${object.company_id.name}<br/>
            % endif
            % if object.message:
                <p>Message : ${object.message}<br/></p>
            % endif
            % if object.request:
                <p>Request/Response : ${object.request}<br/></p>
            % endif

            </p>
                ]]>
            </field>
        </record>

    </data>
</odoo>
